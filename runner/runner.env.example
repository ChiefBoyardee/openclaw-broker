# OpenClaw Runner — copy to runner.env and set values. Do not commit runner.env.
# WSL: place at /opt/openclaw-runner/runner.env and point start.sh (or set RUNNER_ENV).

# Broker base URL (no trailing slash)
BROKER_URL=http://127.0.0.1:8000

# Same token as broker WORKER_TOKEN (required)
WORKER_TOKEN=your_worker_token_here

# Optional: worker identity (default: hostname). Sent as X-Worker-Id to broker.
# WORKER_ID=my-wsl-runner

# Optional: state dir for plan files (plan_echo/approve_echo). Default: /var/lib/openclaw-runner/state
# RUNNER_STATE_DIR=/var/lib/openclaw-runner/state

# Long-poll interval when no job available (seconds)
POLL_INTERVAL_SEC=10

# Optional: max time to wait for a single job result (seconds)
RESULT_TIMEOUT_SEC=300

# --- Capability routing (Sprint 5). Optional. ---
# WORKER_CAPS=llm:vllm,repo_tools
# LLM_CAP=llm:vllm

# --- LLM tool loop (Sprint 5). Required for llm_task jobs. ---
# LLM_PROVIDER=openai_compat
# LLM_BASE_URL=http://127.0.0.1:8000/v1
# LLM_API_KEY=
# LLM_MODEL=your-model-name
# LLM_TEMPERATURE=0.2
# LLM_MAX_TOKENS=4096
# LLM_TOOL_LOOP_MAX_STEPS=6
# LLM_ALLOWED_TOOLS=repo_list,repo_status,repo_last_commit,repo_grep,repo_readfile,plan_echo,approve_echo

# --- Repo commands (Sprint 3). Optional; defaults below. ---
# Base path for repos (e.g. WSL /home/jay/src)
# RUNNER_REPOS_BASE=/home/jay/src
# Allowlist: repo name -> relative or absolute path. If missing/unreadable, fallback: RUNNER_STATE_DIR/repos.json
# RUNNER_REPO_ALLOWLIST=/etc/openclaw/repos.json
# Timeout for git/rg subprocesses (seconds)
# RUNNER_CMD_TIMEOUT_SECONDS=15
# Max bytes for command output (e.g. status porcelain, grep matches)
# RUNNER_MAX_OUTPUT_BYTES=20000
# Max file size for repo_readfile (bytes)
# RUNNER_MAX_FILE_BYTES=200000
# Max lines for repo_readfile output
# RUNNER_MAX_LINES=400
