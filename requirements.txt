# OpenClaw — shared requirements.
# Broker: fastapi, uvicorn, pydantic
# Runner: requests, openai (OpenAI-compatible LLM)
# Bot: discord.py, requests
# Tests: pytest

fastapi
uvicorn[standard]
pydantic
requests
openai
discord.py
pytest
