# Enhanced Runner Dependencies for Urgo
# Install with: pip install -r requirements-runner-enhanced.txt

# Browser automation
playwright>=1.40.0

# HTTP client (already in base requirements, but ensuring version)
requests>=2.31.0

# Note: GitHub tools use only standard library (urllib)
# Note: VPS website tools use only standard library

# Embedding support for remote embedding jobs from Discord Bot
# The runner loads models like Qwen/Qwen3-Embedding-0.6B to process embed commands
sentence-transformers>=2.2.0
numpy>=1.24.0

# PyTorch with CUDA support (install appropriate version for your system)
# For CUDA 12.1: pip install torch --index-url https://download.pytorch.org/whl/cu121
# For CPU only: pip install torch --index-url https://download.pytorch.org/whl/cpu
