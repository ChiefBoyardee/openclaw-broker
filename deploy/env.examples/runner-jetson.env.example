# OpenClaw Runner — Jetson Orin example. Copy to runner.env and set values. Do not commit.

# Broker (no trailing slash). Use VPS or Tailscale IP.
BROKER_URL=http://127.0.0.1:8000
WORKER_TOKEN=your_worker_token_here

# Worker identity and capabilities (broker routes llm_task jobs by caps).
WORKER_ID=openclaw-jetson-orin
WORKER_CAPS=llm:jetson,repo_tools

# LLM (OpenAI-compatible vLLM or other on Jetson)
LLM_PROVIDER=openai_compat
LLM_BASE_URL=http://127.0.0.1:8080/v1
LLM_API_KEY=
LLM_MODEL=your-jetson-model-name
LLM_TEMPERATURE=0.2
LLM_MAX_TOKENS=2048
LLM_TOOL_LOOP_MAX_STEPS=6

# Repo (optional)
# RUNNER_REPOS_BASE=/home/nvidia/src
# RUNNER_REPO_ALLOWLIST=/etc/openclaw/repos.json

# Poll and timeouts
# POLL_INTERVAL_SEC=10
