# OpenClaw Discord Bot — copy to bot.env and set values. Do not commit bot.env.
# Production: /opt/openclaw-bot-<instance>/bot.env (created by onboard_bot.sh or manually).

# Discord bot token (Discord Developer Portal → Your App → Bot → Reset Token / Copy)
DISCORD_TOKEN=your_discord_bot_token_here

# Broker base URL. Same host: http://127.0.0.1:8000. Tailscale: http://100.x.x.x:8443
BROKER_URL=http://127.0.0.1:8000
# Same value as broker BOT_TOKEN (X-Bot-Token)
BOT_TOKEN=your_bot_token_here

# Instance name (optional; systemd sets INSTANCE_NAME=%i for templated units)
# INSTANCE_NAME=default

# Your Discord user ID (Developer Mode → right-click your user → Copy ID). One of allowlist required.
ALLOWED_USER_ID=123456789012345678
# Optional: more user IDs, comma- or space-separated
# ALLOWLIST_USER_ID=111,222 333

# Optional: single channel ID to allow (leave empty for DMs only)
# ALLOWED_CHANNEL_ID=

# Polling and guardrails (optional)
# JOB_POLL_INTERVAL_SEC=2
# JOB_POLL_TIMEOUT_SEC=120
# BOT_COOLDOWN_SECONDS=3
# BOT_MAX_CONCURRENT=1
